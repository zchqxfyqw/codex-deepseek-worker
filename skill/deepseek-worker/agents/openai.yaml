interface:
  display_name: "DeepSeek Worker"
  short_description: "Delegate bounded work to DeepSeek V4 Flash"
  default_prompt: "Use $deepseek-worker for this bounded task. Choose audit, implement, or quota-first from the request and risk, then judge the outcome from runner facts and treat summary.txt only as a concise model summary."

policy:
  allow_implicit_invocation: false
