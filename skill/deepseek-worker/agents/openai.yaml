interface:
  display_name: "DeepSeek Worker"
  short_description: "Delegate bounded work to DeepSeek V4 Flash"
  default_prompt: "Use $deepseek-worker for this bounded task. Invoke only the installed current Runner through PowerShell 7, inspect its bounded evidence, verify task acceptance separately from runner completion, and stop on failure unless the user authorizes takeover."

policy:
  allow_implicit_invocation: false
